<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    android:background="?android:attr/windowBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:padding="8dp">

            <!-- 시설명-->
            <TextView
                android:id="@+id/tvFacilityName"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="시설명"
                android:textSize="30dp"
                android:textStyle="bold"
                android:textColor="#FF4081"
                android:maxLines="1"
                android:ellipsize="end" />

            <!-- 버튼: 고정된 크기로 우측에 고정 -->
            <Button
                android:id="@+id/btnSave"
                android:layout_width="45dp"
                android:layout_height="45dp"
                android:layout_marginStart="8dp"
                style="@style/ButtonTint"
                android:text="+"
                android:textColor="#FFFFFF"
                android:textSize="24dp" />
        </LinearLayout>


        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#CCCCCC"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="8dp" />

        <!-- 2. 시설 세부유형 -->
        <TextView android:text="시설 세부유형" style="@style/Label" />
        <TextView android:id="@+id/tvFacilityType" style="@style/Content" />

        <!-- 3. 시설 지원유형 -->
        <TextView android:text="시설 유형" style="@style/Label" />
        <TextView android:id="@+id/tvFacilitySupportType" style="@style/Content" />

        <!-- 4. 대표자 -->
        <TextView android:text="대표자" style="@style/Label" />
        <TextView android:id="@+id/tvRepresentative" style="@style/Content" />

        <!-- 5. 운영법인 -->
        <TextView android:text="운영법인" style="@style/Label" />
        <TextView android:id="@+id/tvOperatingBody" style="@style/Content" />

        <!-- 6. 설치주체 -->
        <TextView android:text="설치주체" style="@style/Label" />
        <TextView android:id="@+id/tvInstallationType" style="@style/Content" />

        <!-- 7~8. 시/도, 구/군 -->
        <TextView android:text="지역 (시/도, 구/군)" style="@style/Label" />
        <TextView android:id="@+id/tvLocation" style="@style/Content" />

        <!-- 9. 도로명 주소 -->
        <TextView android:text="도로명 주소" style="@style/Label" />
        <TextView android:id="@+id/tvAddress" style="@style/ActionContent" />

        <!-- 10. 지번 주소 -->
        <TextView android:text="지번 주소" style="@style/Label" />
        <TextView android:id="@+id/tvLotAddress" style="@style/Content" />

        <!-- 11. 홈페이지 -->
        <TextView android:text="홈페이지" style="@style/Label" />
        <TextView android:id="@+id/tvHomepage" style="@style/ActionContent" />

        <!-- 12. 대표전화 -->
        <TextView android:text="전화번호" style="@style/Label" />
        <TextView android:id="@+id/tvPhone" style="@style/ActionContent" />

        <!-- 13. 팩스 -->
        <TextView android:text="팩스" style="@style/Label" />
        <TextView android:id="@+id/tvFax" style="@style/Content" />

        <!-- 14. 이메일 -->
        <TextView android:text="이메일" style="@style/Label" />
        <TextView android:id="@+id/tvEmail" style="@style/ActionContent" />

        <!-- 15. 사업자번호 -->
        <TextView android:text="사업자번호" style="@style/Label" />
        <TextView android:id="@+id/tvBizNumber" style="@style/Content" />

        <!-- 16. 지원내용 -->
        <TextView android:text="지원내용" style="@style/Label" />
        <TextView android:id="@+id/tvSupportContents" style="@style/Content" />

        <!-- 17. 시설 평가등급 -->
        <TextView android:text="평가등급" style="@style/Label" />
        <TextView android:id="@+id/tvEvalGrade" style="@style/Content" />

        <!-- 18~19. 정원 -->
        <TextView android:text="정원" style="@style/Label" />
        <TextView android:id="@+id/tvCapacity" style="@style/Content" />

        <!-- 20. 운영여부 -->
        <TextView android:text="운영 여부" style="@style/Label" />
        <TextView android:id="@+id/tvIsOperating" style="@style/Content" />

        <!-- 21. 입소 대상 -->
        <TextView android:text="입소 대상" style="@style/Label" />
        <TextView android:id="@+id/tvEntryTarget" style="@style/Content" />

        <!-- 22. 입소 기간 -->
        <TextView android:text="입소 기간" style="@style/Label" />
        <TextView android:id="@+id/tvEntryPeriod" style="@style/Content" />

        <!-- 23. 입소 절차 -->
        <TextView android:text="입소 절차" style="@style/Label" />
        <TextView android:id="@+id/tvEntryProcess" style="@style/Content" />

        <!-- 24. 주차시설 -->
        <TextView android:text="주차 가능 여부" style="@style/Label" />
        <TextView android:id="@+id/tvHasParking" style="@style/Content" />

        <!-- 25. 주변 교통 -->
        <TextView android:text="지하철역" style="@style/Label" />
        <TextView android:id="@+id/tvNearbySubway" style="@style/Content" />

        <TextView android:text="버스정류장" style="@style/Label" />
        <TextView android:id="@+id/tvNearbyBusStop" style="@style/Content" />

        <!-- 26. 생성일 -->
        <TextView android:text="최종 업데이트일" style="@style/Label" />
        <TextView android:id="@+id/tvCreatedDate" style="@style/Content" />

    </LinearLayout>
</ScrollView>
